# This Python file uses the following encoding: utf-8

import os

class connection:
    def __init__(self, self.user, self.passwd):
        usuario =
        senha =

    def createConnection(self ):
        os.command(ssh, self.usuario )
        if():
            ("não foi possivel conectar a sua vps informada, por favor tente outro usuario e/ou senha")
            return False

        return True

